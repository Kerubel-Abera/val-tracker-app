ext {
    // Retrofit
    retrofitVersion = '2.9.0'
    okHttpVersion = '4.5.0'
    gsonVersion = '2.9.0'

    // Kotlin Coroutines
    coroutinesVersion = '1.6.4'

    // Dagger-Hilt
    hiltVersion = '2.46.1'

    // Jetpack Compose
    composeVersion = '2022.10.00'
    navigationVersion = '2.6.0'

    // Coil
    coilVersion = '2.4.0'

    // AndroidX Core and Activity Compose
    coreKtxVersion = '1.8.0'
    lifecycleVersion = '2.3.1'
    activityComposeVersion = '1.5.1'

    // LiveData
    liveDataVersion = '1.4.3'

    // SystemUI accompanist
    systemUIControllerVersion = '0.31.3-beta'

    // Splashscreen
    coreSplashscreenVersion = '1.0.1'

    // Testing
    junitVersion = '4.13.2'
    junitExtVersion = '1.1.3'
    espressoVersion = '3.4.0'
}
